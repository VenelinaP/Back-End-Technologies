function sum(input) {

    let firstNum = input[0];
    let lastNum = input[input.length -1]; //така взима последния елемент от масива

    console.log(firstNum + lastNum);
}
sum([20, 30, 40]);